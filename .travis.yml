language: python
python:
  - "2.7"
  - "3.3"
  - "3.4"
  - "3.5"
  - "3.6"
  - "3.7"
  - "3.8"
# command to install dependencies
install: "pip install . coveralls"
# command to run tests
script:
  coverage run --source=fsed setup.py test
after_success:
  coveralls
  
